# The origin of the open-ticket API (reads, SSE, command POSTs). Inlined by Next at build time,
# so it is public. The API must allow this web app's origin via its WEB_ORIGIN (CORS, D3-05).
NEXT_PUBLIC_API_URL=http://localhost:5210
